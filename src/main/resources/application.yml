spring:
  datasource:
    url: jdbc:mysql://localhost:3306/competition_subject?useUnicode=true&zeroDateTimeBehavior=convertToNull&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8&tinyInt1isBit=false
    username: ENC(GNTGqKTs2Iu5LCeKJMXiYQ==)
    password: ENC(IBZ2wVgOEI3bT4BpDVvN9g==)
    driver-class-name: com.mysql.jdbc.Driver

  # redis配置
  redis:
    port: 6379
    password:
    database: 1
    jedis:
      pool:
        max-active: 8
        min-idle: 0
        max-idle: 8
        max-wait: 3000
    timeout: 10000



server:
  port: 8888
  servlet:
    context-path: /subject

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
  global-config:
    typeAliasesPackage: com.gxuwz.subject.model
    db-config:
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)
  mapper-locations: classpath:mapper/*.xml


# 全局异常处理：抛出后会被GlobalExceptionHandler.class处理
mvc:
  #出现错误时, 直接抛出异常
  throw-exception-if-no-handler-found: true

jasypt:
  encryptor:
    # 盐
    password: tale666cqf88miracle6TALE_CQF
#    property:
      # 自定义属性规则，默认前缀是“ENC(”，后缀为“)”
#      prefix: "ENC["
#      suffix: "]"


# 监控服务
management:
  server:
    port: 1688
  endpoint:
    shutdown:
      enabled: true

 # 监控信息
info:
  app:
    name: tale_监控
    url: https://www.jianshu.com/u/7f093d6ed255
    email: ihopyou@qq.com


